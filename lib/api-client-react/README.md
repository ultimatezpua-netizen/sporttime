# @workspace/api-client-react (stub)

This is a minimal stub of the `@workspace/api-client-react` package added to make the pnpm workspace resolution work in CI. Replace with the real package sources from the original monorepo when available.
